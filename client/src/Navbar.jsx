export default function Navbar() {
    return <nav className="nav">
        < a href="/" className="site-title"> Credit Card Matcher </a>
        <ul className="active">
            <li className="nav-item">
            </li>
        </ul>
    </nav>
}
// when they are on top of each other the elements they get stacked